.container-fluid.full-height.bg-grey.py-1
  .form-wrapper.mx-auto.my-5.p-4.text-center
    %h2
      Profile settings
    - if resource.provider.nil?
      = render 'standard_user'
    - else
      = render 'oauth_user'
    = link_to 'Delete my account', registration_path(resource_name), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger'
    .mt-2
      = link_to 'Back', :back
